<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AliBabaInternationalTask" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="BtnPrimary" parent="Theme.AliBabaInternationalTask">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/bg_btn_primary</item>
        <item name="android:paddingStart">@dimen/padding</item>
        <item name="android:paddingEnd">@dimen/padding</item>
        <item name="android:textSize">@dimen/btn_text_size</item>
    </style>

    <style name="TxtTitle" parent="Theme.AliBabaInternationalTask">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/text_normal</item>
    </style>

    <style name="TxtDesc" parent="Theme.AliBabaInternationalTask">
        <item name="android:textColor">@color/gray500</item>
        <item name="android:textSize">@dimen/text_small</item>
    </style>

    <style name="CircularImage">
        <item name="cornerSizeBottomLeft">@dimen/card_view_radius</item>
        <item name="cornerFamilyBottomLeft">rounded</item>
        <item name="cornerSizeTopLeft">@dimen/card_view_radius</item>
        <item name="cornerFamilyTopLeft">rounded</item>
    </style>
</resources>